apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: registrydeployments.agentregistry.dev
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
spec:
  group: agentregistry.dev
  names:
    kind: RegistryDeployment
    listKind: RegistryDeploymentList
    plural: registrydeployments
    singular: registrydeployment
    shortNames:
      - regdeploy
      - rdeploy
  scope: Cluster
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Resource
          type: string
          jsonPath: .spec.resourceName
        - name: Version
          type: string
          jsonPath: .spec.version
        - name: Type
          type: string
          jsonPath: .spec.resourceType
        - name: Runtime
          type: string
          jsonPath: .spec.runtime
        - name: Phase
          type: string
          jsonPath: .status.phase
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
      schema:
        openAPIV3Schema:
          description: RegistryDeployment is the Schema for the registrydeployments API
          type: object
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              description: RegistryDeploymentSpec defines the desired state of RegistryDeployment
              type: object
              required:
                - resourceName
                - version
                - resourceType
                - runtime
              properties:
                resourceName:
                  description: ResourceName is the name of the resource in the catalog
                  type: string
                version:
                  description: Version is the version of the resource to deploy
                  type: string
                resourceType:
                  description: ResourceType is the type of resource (mcp, agent)
                  type: string
                  enum:
                    - mcp
                    - agent
                runtime:
                  description: Runtime is the deployment runtime (local, kubernetes)
                  type: string
                  enum:
                    - local
                    - kubernetes
                preferRemote:
                  description: PreferRemote indicates whether to prefer remote transport
                  type: boolean
                config:
                  description: Config contains deployment configuration
                  type: object
                  additionalProperties:
                    type: string
                namespace:
                  description: Namespace is the target namespace for Kubernetes deployments
                  type: string
            status:
              description: RegistryDeploymentStatus defines the observed state of RegistryDeployment
              type: object
              properties:
                phase:
                  description: Phase is the current deployment phase
                  type: string
                  enum:
                    - Pending
                    - Running
                    - Failed
                deployedAt:
                  description: DeployedAt is the timestamp when the deployment was created
                  type: string
                  format: date-time
                updatedAt:
                  description: UpdatedAt is the timestamp when the deployment was last updated
                  type: string
                  format: date-time
                message:
                  description: Message is a human-readable message about the current status
                  type: string
                managedResources:
                  description: ManagedResources lists the Kubernetes resources created
                  type: array
                  items:
                    type: object
                    required:
                      - apiVersion
                      - kind
                      - name
                    properties:
                      apiVersion:
                        type: string
                      kind:
                        type: string
                      name:
                        type: string
                      namespace:
                        type: string
                conditions:
                  description: Conditions represent the latest observations
                  type: array
                  items:
                    type: object
                    required:
                      - type
                      - status
                    properties:
                      type:
                        type: string
                      status:
                        type: string
                      lastTransitionTime:
                        type: string
                        format: date-time
                      reason:
                        type: string
                      message:
                        type: string
                observedGeneration:
                  description: ObservedGeneration is the generation last observed
                  type: integer
                  format: int64
